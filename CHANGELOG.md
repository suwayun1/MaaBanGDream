# 变更记录

## Unreleased

- 新增「团队演出 Fes」任务骨架：interface.json 注册任务与难度/次数/诊断选项，`resource/pipeline/fes_live.json` 复用进程冲突守卫、主页恢复、流速门禁和结算上报链路；新增 `agent/realtime/fes_action.py`，演奏委托 `RealtimeProfilePlay`（`run_mode=fes`，录像前缀 `fes`）。次数 0–999、0 为无限，Special 缺席时回退 Expert。活动入口按 OCR 文本「团队演出」识别；房间创建/加入的界面识别待真机截图补充，当前要求启动时设备已处于可进入准备页的状态，未经真机验收。

## 2026-09-17（v1.4.3）

- 所有带次数输入的演出任务支持 0–999 次，0 表示无限运行直到用户停止或真实失败；组曲有限次数仍须为 3 的倍数，无限模式按完整三首循环。普通次数门禁不再直接用输入值覆盖 `max_hit`，进度明确显示“无限”，组曲跨组与重试改用迭代以避免长期挂机栈溢出。
- 演出设置中的单局技术失败重试上限从 3 提高到 99，默认仍为 1；同步 Python 校验、协力预算、界面加载和数字控件。

- 修正组曲快速结算后的主页退出弹窗竞态：点击取消后暂停 BACK，被动确认无弹窗主页连续稳定；主页短暂命中也先确认送达，避免关闭动画或新弹窗出现前的旧画面误报恢复成功。进入下一组巡回演出前再检查退出弹窗，不恢复中间结算页的固定慢等待。
- 移除组曲结算三相输入中的固定 850/150/850 毫秒等待及结算后主页恢复的 500 毫秒轮间等待，并使用前后延迟均为零的专用截图刷新节点，输入回执后立即刷新检查；不改变选曲、准备和重启后登录流程，保留停止、前台保护、三张成绩顺序和必要终点识别，成绩数字稳定检查仍只在未启用“不检查结果”时执行。
- 演出已确认结束且完成触点清理后，成绩识别、数字不稳定、结果截图、报告保存及结算恢复异常只记录警告，不撤销本局完成或终止后续演出；真实死亡、开演前硬冲突、未完成演出和用户停止保持独立处理。
- 演出设置新增默认关闭的“不检查结果”，跳过普通演出和组曲的 PGGBM 判定数字检查/记录，保留必要终点识别与安全页面推进。实时校准仍必须取得成绩验收 Profile，已完成演出的结算技术错误按既有预算重试，耗尽后中性保留可续跑阶段，不接受缺少成绩的 Profile。
- 组曲结果输入改为显式三阶段“最右下角安全像素 → Android BACK → 同一安全像素”，每步后重新检查页面；已确认三首完成时不再因缺失延迟报告或 PGGBM 数字读取失败结束整项任务，不伪造缺失成绩。

## 2026-09-16（v1.4.2）

- 将主分支合并规范改为通过 PR squash 合并，与 GitHub 线性历史规则一致；同步贡献指南，禁止强制推送或改写共享历史。
- 组曲自由巡演进入每首歌曲选择页前增加五枚大圆难度按钮布局确认与最多三次有界重试，连续稳定两帧后才允许随机选曲或选择难度；点击请求成功不再代表页面已经送达。每次组曲难度输入前再复核页面，标准难度颜色检测也会拒绝总览页等不匹配布局，防止把综合力红字误报为 Easy。三次打不开时保存现场并报告明确的选曲页未送达原因，用户停止不继续重试或记录业务失败。
- 删除组曲三首完成后 PGGBM 标题、难度和等级的重复 OCR 身份门禁：沿用开演前及最终封面已确认的本局身份，三张成绩按会话第 1→2→3 首顺序保存；保留 PGGBM 页面标记、数字稳定性、谱面总判定数校验及上一页已离开的翻页确认。单人、挑战、校准、一键实时和协力共用的结算器同样只消费成绩页面，不重新以结果页标题否决已完成的演奏，并新增回归约束。
- 修正组曲数字稳定计时每帧重置的问题：相同数字连续累计稳定时长，仅在数字变化、PGGBM 标记消失或解析失败时重新计时，避免删除耗时标题 OCR 后稳定数字仍一直超时；按谱面总判定数修复后仍不一致的回退读数继续等待，不直接保存为已校验成绩。

- 单人及组曲准备页的歌曲身份缺失或冲突改由本局开场最终封面与实际 OCR 标题独立兜底，隔离旧谱面并延迟 Native 预武装；最终确认后更新本局身份，最终仍无法确认时不使用冲突谱面输入。
- 补充已验证的 `蒼穹へのトレイル`、`黒のバースデイ` Expert 区服等级差异兼容，并在谱面同步中区分源等级与 CN 等级。例外绑定歌曲、难度和谱面 SHA；其它等级冲突仍明确报告，不能以普通封面未匹配掩盖。
- Native 轻度调试新增首次快速掉血前后约两秒、最多 21 张已有生命监控帧的异步取证；队列满时丢弃并计数，不增加截图、同步写盘或触控。`overtuRe` 末段死亡现有证据只确认最后约两秒真实归零，设备命令完整且末段执行偏差较小，未据此改动时序或宣称根因已解决；新增取证仍需下一次故障现场验证。
- 雷电真机已完成 Hard 自由巡演连续三组、共九首，三张成绩逐组完整保存，并以本次要求的 `9/9` 成功返回主页；这次验收覆盖跨组选曲、难度确认和结算，不代表偶发死亡根因或所有模式已验证。

## 2026-09-15（v1.4.1）

- 精简 `AGENTS.md`：删除已完成修复的逐次复盘、历史提交/测试数字和重复流程说明，改为按项目边界、长期生命周期约束、各模式关键规则、定制 MFA 保护与当前未验收范围组织；保留本轮组曲 task ID 隔离、整组三曲失败重试和专用退出页规则，避免常驻 Agent 上下文被历史记录持续占满。
- 修复重新点击开始组曲后复用上一 Maa 任务的暂停会话、错误继承 `6/9` 进度的问题：组曲会话现持久化外层 `task_detail.task_id`，仅相同 task ID 才允许第 2/3 曲、结算或失败重开续跑。新任务遇到旧 schema 会话或不同 task ID 时会标记为 `new_task_started` 并从 `0/目标次数` 创建新组；缺少稳定 task ID 时 fail-closed，绝不以临时 RemoteTasker 指针替代。新任务若已停在旧会话的进行中准备页，也不会把旧曲计入新配额。
- 修复组曲空血重试时两层退出弹窗沿用共享 ROI 而识别不到第一层的问题：新增组曲专用的第一层灰色“退出”、第一层粉色“继续”和第二层粉色“退出”节点，所有非结算主页恢复均传给 `CommonRecover`；若已停在第二层确认页，安全恢复后仍按严格先左后右的两阶段状态机处理。结算继续 back-only；共享单人节点不改，避免把第一层星石“继续”误当第二层确认退出。
- 修复组曲中单曲生命归零后直接以 `6/9`（或当前已完成数）失败、无法继续的问题：现在会读取该曲的延迟/即时结果报告与最近失败原因；即使 Native 完整性门禁把已确认的“生命值归零”二次标为 `engine_error`，也会识别为可重试。失败后会先把任务进度精确恢复到本组三曲开始前（例如第 2/3 曲失败仍回到完成 `6/9`、当前 `7/9`），避免重开组或重试耗尽时误计已完成歌曲。重试次数复用“演出失败重试次数”设置；每次先释放 Native 预武装，再由 `CommonRecover` 点击失败弹窗左侧“退出”并恢复主页，把失败组三曲会话标记为 `superseded`，从本组开始前的完成数重新演奏完整三曲。失败曲和本组未完成曲不计数，不会点击星石“继续”；Profile/身份/谱面/流速硬冲突和用户停止不重试，完整三曲结算后才重置预算。

## 2026-09-15（v1.4.0）

- 将 MaaBanGDream 自有部分从 GPL-3.0-only 调整为 PolyForm Noncommercial 1.0.0：允许非商业查看、克隆、运行、研究、修改和分发，禁止收费软件、收费分发、收费部署或维护、商业服务、商业产品集成及其他预期商业应用；`v1.3.9` 及更早标签继续按各版本随附的 GPL 授权。
- 新增独立许可范围、第三方权利和品牌政策文档。官方项目继续使用 MaaBanGDream 名称；公开派生版本须使用不同名称与 Logo，不得借项目名、Logo 或作者身份宣传收费服务或暗示官方背书，正常讨论、教程、引用及来源说明不受限制。
- Windows 完整包与更新包新增 MaaFramework LGPL-3.0 正文、MaaBanGDream 许可范围、品牌政策和第三方权利说明；定制 MFAAvalonia、MaaFramework、minitouch、nlohmann/json、OCR 模型和游戏素材继续保留各自许可证或权利状态。
- 修复组曲启动时仅凭旧“待结算”会话就把任意非主页页面当成结算续跑的问题：只有第 2/3 曲准备页仍按身份约束直接续跑；普通非主页页面会沿用安全像素/BACK 节拍返回主页，无法继续读取的旧结算标记为不可恢复，再从第一曲开始新一组，不会先伪报 `3/3` 后失败。2026-09-15 雷电真机已验证第 2 曲断点续跑至三张 PGGBM 全部保存并以 `3/3` 成功回主页；随后从普通非主页页面启动时两次 ESC 恢复主页并从新一组 `1/3` 开始。
- 修复组曲曲间等待偶发 `ValueError: sleep length must be non-negative`：等待循环现在只计算一次剩余时间，并在截止时直接返回，避免循环条件与 `time.sleep()` 之间跨过截止点后传入负数；正式演出预检查中的同型等待也同步加固。
- 修复正式演奏准备页在 3D 演出状态下反复点击成员头像、无法关闭演出显示的问题：3D 演出时旧 Cut-in 检测区会显示粉色成员头像，现严格先用左下循环按钮依次切换 `3D演出 → 动画MV → OFF`，确认 OFF 后才读取并按需关闭出现的“3D Cut in模式”复选框；各步增加明确动作日志。

## 2026-09-14（v1.3.9）

- 统一所有演出任务的结算输入：每轮严格执行最右下角安全像素 `(1279,719)` → Android BACK → 同一安全像素，像素点击只加快动画，BACK 是唯一推进页面的输入；不再点击奖励、排名、活动、确定或下一步等可见按钮，也不识别中间结算页。普通演出读取一张 PGGBM，组曲按会话断点读取总计三张；组曲在每次安全像素或 BACK 输入后都会先截图检查，避免一整轮输入连发时越过刚出现的 PGGBM。结算耗尽后统一保存现场并调用 `CommonRecover`，必要时重启游戏。一键实时演奏也会读取 PGGBM 并恢复主页后再结束任务。每次结算输入会在前台保护完成后重新取得当前反向 Controller，避免嵌套演奏替换代理后复用旧句柄而触发原生访问冲突。
- 修复第三曲结束后停止任务、再由用户手动退出结算时，旧组曲会话永久阻塞后续任务的问题：重新启动任务若已在主页或巡演选择页，少于三张 PGGBM 时会把旧会话保留为不可恢复记录并从第一曲开始新一组，不会补算缺失结果或继续报错；三张均已原子保存时只补记上一组完成状态，不会重打。

## 2026-09-14（v1.3.8）

- “组曲演奏”的次数改为可输入：一首歌记 1 次，当前接受 3–99 且为 3 的倍数；每组三首完整结算后回主页继续下一组，不把第 1/2 首后的未完成组计为成功。
- 修复自由巡演在第 4 张图提前读取歌曲身份并拒绝重复歌曲的问题。该页现在只确认请求/实际难度，连续相同歌曲可继续；三个槽位先保存难度、Profile 和流速，每首到自己的准备页才填充身份。
- 准备页标题或完整身份缺失时，最终歌曲信息页会用封面和实际 OCR 标题在触控前补全，并把实际标题、置信度和来源写回原子组曲会话；仍无法确认则停止输入。
- 新增“组曲演奏”首版：完成三首自由巡演或课题巡演。自由巡演支持当前曲目/每首随机和统一难度；课题巡演只读三首预设歌曲和难度。
- 组曲三首开始前分别复核封面、标题、等级、难度与 Profile，三份 Profile 流速不一致时开演前阻止；流速开关开启时，课题巡演会在读取阵容后回主页检查一次，再重新进入确认阵容未变化，中途不打开设置页。
- 新增独立的原子组曲会话与第 2/3 曲身份约束续跑；会话位于 `profiles/medley-sessions/`，不修改旧 Profile、旧校准文件或校准会话读取规则。用户停止保持中性状态，缺少会话或身份冲突时 fail-closed。
- `RealtimeProfilePlay` 支持组曲延迟结算：三首演奏分别生成 run ID 和待补全报告，第三首后按标题、等级、难度顺序读取三张 PGGBM，逐首保存结果并沿用正式演奏的 timing offset 回写规则。
- 组曲结算新增实拍“达成报酬一览”和组曲分数汇总页识别：前两曲弹窗只发送一次 Android BACK，第三曲后先离开汇总页再依次读取三张 PGGBM，避免在转场帧连续 BACK 跳过结果页或误触主页退出确认。
- 已知结算页先发送 Android BACK，页面未离开时再点击右下角 `(1065,650)`；持续未知页面会交替发送 BACK 与右下角点击，每次输入后重新识别。达到有界次数仍无法离开时保存现场并执行 `CommonRecover`，必要时重启游戏恢复主页，同时保留任务失败结果。
- 2026-09-14 已在雷电 Native 完成一组自由巡演“当前曲目”真机验收：三首重复歌曲均从各自准备页确认身份，三份结果均为 713P/0G/0M，三张 PGGBM 保存后处理活动故事弹窗并回到主页，MFA 以 3/3 成功结束。随机歌曲、课题巡演、跨组计数和中断续跑尚未据此宣称真机通过。
- README 补充雷电模拟器 9.5.30.1 下载链接；当前 MuMu 实机测试样本较少，实时演奏优先推荐使用已有较多验证记录的雷电模拟器。
- “一键实时演奏”不再从歌曲中途的生命条画面启动；现在会提示用户开始识别，并等待连续稳定的开场封面与标题确认本地曲目，再按任务所选难度进入演奏。有对应谱面时可继续使用 Native，无谱面难度保持整局视觉 Legacy。
- 一键演奏的难度与诊断选项改由两个独立配置节点合并，修复同时选择 Expert 与轻量 Trace 时，后者整块覆盖难度参数并使实际演奏错误回落 Easy 的问题。
- 兼容 MaaFramework 对未配置 `custom_action_param` 的节点传入 JSON `null`；一键监听节点会将其规范为空对象，不再在开始识别后因 `**None` 立即失败。
- 一键实时演奏从“持续监听多首、用户手动停止”改为单曲任务：只有演奏引擎确认本局完成才返回成功，随后进入明确的成功终态并自动结束，不再重新监听下一首。
- 一键监听阶段增加有界识别诊断：每两秒记录封面指纹、稳定帧、标题 OCR、拒绝原因和演奏场状态，停止或失败时额外保存最多八张稳定候选帧，避免只剩终态画面而无法区分漏封面、标题冲突、曲库缺失或启动过晚。
- 修复一键监听仍以严格 8 bit 门槛拒绝 `Little Busters!` 开场封面的问题：实拍封面距曲库标准指纹 10 bit，现仅在标题已独立收窄候选后放宽到 14 bit，并用确认后的曲库标准指纹解析任务难度谱面；标题不匹配的近似封面仍会拒绝。

## 2026-09-13（主页演出设置门禁修正）

- 自动设置流程收缩为只处理音符流速：先点击主页菜单与“选项”，再读取、按需修正并复核流速；镜像、判定辅助、连击显示、FAST/SLOW、NOTE TYPE 与 TAP EFFECT 不再检查或修改，原视觉评估任务和对应模板已删除。
- MFA“演出设置”删除流速以外的游戏设置控件，仅保留“开演前自动设置并验证流速”和各难度目标流速；README 记录其余设置的人工推荐值。
- 删除跨任务持久设置凭据；流速开关开启时每次任务都真实进入主页设置页，准备页只接受本任务主页结果且永不打开设置。关闭时单人、协力、挑战、校准和一键实时演奏均不检查设置页。
- 保留 Profile/校准 JSON 版本 1 中的旧视觉字段读取兼容，但这些字段不再参与 Profile 匹配或校准会话续跑判定；旧的 `game_effect_settings_enabled` 在读取时迁移为 `note_speed_settings_enabled`。

## 2026-09-12（图像分析工具路由）

- 取消 MaaBanGDream 对外部 `vision` skill 的一刀切强制：模型可直接理解用户附图时直接分析；简单 OCR、文字提取、单模板确认和批量帧初筛仍可使用 vision。
- 复杂页面、控件空间关系、点击坐标、Computer Use 当前界面，以及 vision 结论不完整、矛盾或不能解决问题时，明确允许并优先使用原生 `view_image` 或 Computer Use 屏幕上下文；多图通过裁剪、缩放和限制必要帧控制上下文，不再全面禁用原生看图。

## 2026-09-12（开演前设置合并检查）

- 单人、协力、挑战与校准统一在离开主页前完成流速、演出特效与皮肤检查；准备页只复用本任务主页结果并处理身份/Profile/Native 预武装，禁止再打开设置齿轮。
- 所有设置页滑动统一移到实机确认无按钮的右侧 `x=1000` 空白区，避免翻页手势落在开关上。判定辅助不再用“页面最靠下的二选一”猜测，而是与连击显示及位置行分开读取，修复滚动落点变化时误关连击显示的问题。
- 固定复核并按需修正：镜像关闭、连击数量显示开启且位置为右/上、FAST/SLOW 表示开启；FAST/SLOW 先验证“演出效果·音量设定”页签白底选中，再锁定顶部第二条三列行的中列并读回，避免把滚动后的成员图像切入等其它行误判为目标。这些属于内部演奏环境策略，不增加 MFA 开关；NOTE TYPE、TAP EFFECT、判定辅助和流速继续服从现有演出设置。

## 2026-09-12（Special 缺失时回退 Expert）

- 协力、单人实时、挑战和自动演出选择 Special 后，如果当前歌曲没有 Special 按钮或按钮不可选择，会在三次确认失败后显式改选 Expert，不再直接报错退出；实时校准仍要求精确的 Special 难度，避免用 Expert 谱面生成错误的 Special 校准结果。
- 单人实时与挑战的后续设置门禁、准备页身份、Profile、谱面、Native 预武装和实际演奏统一消费本局已确认的 Expert 难度，同时保留 `requested_difficulty=Special`、`effective_difficulty=Expert` 证据；只有实际选中 Special 时才继续执行 Special 本地谱面和方向语义门禁。
- 新增 Special→Expert 本局难度传递、流速门禁、演奏入口和 interface/MaaFramework 完整参数覆盖回归，防止 Custom Action 参数整块替换后丢失回退配置。

## 2026-09-12（协力首音弹窗误判修复候选）

- 外部 v1.3.7 证据包确认一次协力 Hard 空血不是谱面、Profile 或 minitouch 传输故障：歌曲 538 的真实“其他成员正在准备中”弹窗已经完整出现并消失，首批双 FLICK 又被像素启发式误判为仅持续一帧的第二次弹窗，首音门控随即重置并约 1.066 秒后才触发；该谱面第一组与下一颗音符相隔约 0.985 秒，导致整局输入错后一颗并在三秒内连续 MISS 到空血。
- 根据真实弹窗从固定中心等比例放大、缩小的动画语义，像素检测器把白色主体中心限制在画面高度 60%–70%：不论完整出现还是只闪现一帧都继续拦截，消失帧继续重置判定带；弹窗完全不出现时不会增加额外等待。判定线附近更靠下的白底粉色双 FLICK 不再满足弹窗几何约束，可直接交给首音门控；不再依赖“弹窗必须出现”或“至少持续两帧”的生命周期假设，原有 500ms 前奏宽限、演奏场确认与 broad-change 门禁保持不变。
- 新增弹窗完全不出现、冻结后只闪现一帧、正常完整出现、缩放中间态以及首批双 FLICK 的确定性回归；用户提供的 1280×720 标准比例截图也能被新检测器识别。外部 trace 只从 Native 启动后以约 5Hz 记录，缺少误判瞬间的 60FPS 帧，离线 replay 可读取但不能重演首音门控；候选仍需真实协力双 FLICK 开头歌曲验收。

## 2026-09-12（协力开演前耗时优化）

- 开发 MFA 的 28 次真实协力记录中，从点击难度到点击“演出开始”平均约 3.57 秒，中位数 3.41 秒，范围 3.20–5.72 秒；主要耗时来自关闭设置门禁后仍串行执行的两次完整刷新截图，而不是点击后的送达确认。
- 协力跳过游戏设置页时，流速门禁会保存当次刚采集的准备页画面；演出模式已经关闭且画面中存在“准备完毕”按钮时，模式确认与按钮点击复用该画面，不再各自刷新一次。打开过设置页、演出模式发生切换或缓存画面没有按钮时仍重新截图，防止用旧画面误判状态。
- “准备完毕”点击后的按钮消失、成员退出与黑场高频观察完全保留；本次只缩短点击前的重复取图，未放宽触控送达、开演转场、歌曲身份或谱面门禁。自动化回归已覆盖复用、缺按钮回退刷新、模式切换和 Special→Expert 协力回退。雷电 Native 两局真机均复用本轮准备页、第一次点击送达且正常完成结算；校正难度点击后的 250ms 复核延迟后，难度点击至“演出开始”点击分别约 0.76 秒和 0.43 秒，平均约 0.59 秒，较旧基线缩短约 83%。两局 Native 分别完整执行 3,881/3,881 与 2,304/2,304 个动作，零下溢且触点释放确认成功。

## 2026-09-12（Special 谱面支持）

- 单人实时与挑战演出选择 Special 时保持严格语义：当前歌曲没有 Special 或按钮不可选择会明确失败，不再把其他难度当成 Special；自动演出改为点击后复核实际难度，复核失败时禁止继续开演。
- 协力请求 Special 而当前歌曲不可选时允许显式回退 Expert；请求难度与实际难度分别写入本局状态和结果 JSON，实际 Expert 会贯穿 Profile、流速门禁、本地谱面解析与播放参数。
- Special 实时演奏必须在发送触控前确认可信本地谱面；谱面缺失、身份冲突或最终封面无法确认时 fail-closed。Legacy 也会强制启用本地谱面的 Directional 左右语义，不再用无方向的通用视觉 FLICK 冒充完整 Special 支持。
- Python 与 Native 谱面模型保留并校验 Directional `width=1..7`。完整曲库审计发现歌曲 786 含交接统计遗漏的 `width=4..7`；上游实现表明该字段描述横跨轨道数、判定尺寸及最低滑动阈值，现有水平滑动距离仍覆盖七档，因此候选未凭字段名改动手势距离，也未在没有 Special 真机帧/trace 的情况下修改 190ms 首音补偿或协力 broad-change 门禁。
- 验证：Special 定向回归 249 passed；Native C++ 1827 checks passed；159 张 Special 经 Python/Native 全量解析为 119,774 个判定、8,271 个 Directional 和 655,815 个 Native 动作，失败 0；既有协力 trace 离线 replay 通过且无重复判定；真实 MaaFramework 覆盖验证确认 AutoLive 五档参数完整；主项目完整 `scripts/verify.ps1` 为 1051 passed、7 skipped，固定 MFA 运行时兼容检查通过。候选已由 `launch-mfa.ps1` 部署到开发目录并完成雷电 Native 单人真机验收：五局均为请求/实际 Special、本地谱面与最终封面一致、MISS 0、全动作执行且释放成功，GREAT 依次为 4/0/11/5/1；其中 `Realize` 与 `誓いのWingbeat` 的谱面合计覆盖 236 个 Left/Right Directional 及 `width=1..3`。同曲复测把 11 GREAT 降至 5/1，用户确认按运行波动接受；协力 Special→Expert 回退和 broad-change 门禁仍没有本轮真机房间证据。

## 2026-09-11（v1.3.7）

- 修正 v1.3.7 runtime-free 更新 ZIP 的目录结构：更新包现在直接在归档根目录提供 `interface.json`，并增加结构回归检查，避免 MFA 下载和 SHA-256 校验通过后误报“资源包缺少 interface.json”；MFA 完成界面初始化后会主动执行一次资源版本检查，无需等待手动点击。
- Native 等待成本与一次性启动延迟补偿完成雷电真机验收并默认启用；东京校准取得 852P/7G 与 851P/8G，Talk to My Tone 正式演奏取得 458P/0G，三局均零 MISS、零下溢且设备回读完整。开发环境仍可用 `-DisableNativeTimingCompensation` 临时关闭补偿做回归对照。
- 修复实时校准通过正式验证后仍保留旧 Profile 的问题；新候选会立即成为对应难度的当前选择。普通单人重试预算改用 Maa 的稳定任务 ID，避免临时代理对象重建后重复放行重试。
- “关于我们”使用专属 v1 Logo、项目介绍、联系方式和许可证；Logo 在页面内居中放大裁剪，软件标志继续使用默认 Logo。内容框统一采用 MFA/Suki 原生 `GlassCard`，随主题玻璃透明度变化。
- 完整包与更新包内置当前版本的 `resource/Release.md`；“显示公告”和更新完成弹窗只读取这份本地说明，不再从 GitHub 获取正文。GitHub REST API 明确限流时，稳定通道仍回退发布网页并继续确定性选包和 SHA-256 校验；版本比较兼容带 `v` 的 Release tag。
- Profile 难度兼容把 Expert 与 Special 归为同一高难度等级；表格移除“自动选择”和“钉选当前”按钮，双击行设为当前任务难度的 Profile，并以随主题变化的“当前选择”标记显示。
- 删除生命保护页、运行时选项、低血量阈值提前暂停和挑战专用分流；数值生命监控继续负责演奏场确认、生命归零、结算终态及协力自动跳车。
- 实时校准不再强制检查演出特效，改为服从“开演前自动设置并验证游戏特效”开关；演出设置页改用原生分隔线，五档校准流速上限修正为 12。
- 开发启动器修复目标 MFA 在停止等待期间自行退出导致的误报；开发启动器与发布构建脚本固定使用 UTF-8 BOM，避免 Windows PowerShell 5.1 把中文命令或包内启动器名称错误解码。
- 发布 ZIP 改用显式 UTF-8 条目名生成器，修复 `tar.exe` 把中文启动器和依赖安装脚本写成乱码的问题；发布校验现在直接检查完整包与更新包的 ZIP 中央目录。
- 验证：主项目完整检查 1024 passed、7 skipped；定制 MFA 自动保存、About、本地发布说明、GitHub 更新器和 Profile 设置回归通过，两个仓库差异检查与敏感信息扫描通过。
- 用户已确认资源检查、资源更新和最终 About 页面可用。Native timing 由雷电真机三局验证；演出设置简化后的完整任务流程仍需在发布包上继续验收。

## 当前状态

- 自动演出、连续轮次、随机选曲、挑战演出和实时演奏主流程均已投入使用。
- Bestdori 本地仓库现覆盖 809 首歌曲、1777 张 Hard/Expert/Special 谱面和 867 个封面；开演时不访问网络。
- `scripts/verify.ps1` 与定制 MFA 自动保存测试是提交门禁；实时触控修改还需先离线重放，再由用户真机验收。
- Native Realtime Engine V2 仍是未发布、默认关闭的实验路径；离线实现不代表真机成绩，验收前不会替代 Python Legacy。
- 当前待验收项：Native V2 连续 10 局 Expert 真机门槛、共享封面歌曲的 Normal 真机复验，以及完整校准断点续跑；v1.2.0 的协力弹窗门控、末尾漏键、结算恢复与单人跳过设置页预武装仍需真机复验。

## 2026-09-10（v1.3.6）

- 更新设置统一回到 MFA 原生 `VersionChecker`：删除 MaaBanGDream 自绘 GitHub 更新卡片和并行更新器，复用原生检查、进度、Toast、标题栏提示与自动更新入口；资源未配置 Mirror RID 时只显示并固定使用 GitHub。
- GitHub Release 资产按系统、架构和本机运行库状态确定性选择：现有 Python runtime 优先约 148 MiB 的 `-update.zip`，运行库缺失时回退完整包，`.sha256` 不再可能被误选为更新归档；GitHub asset digest 缺失时读取同名校验文件。
- 原生下载器增加 `.part` 与 HTTP Range 断点续传，兼容 206、忽略 Range 的 200 和已完成分片的 416；SHA-256 通过后才形成正式下载文件。
- 含 MFA 核心文件的便携包完成验证后，由 self-contained `MFAUpdater.exe` 等待主进程正常退出再覆盖；保留 config/profiles/debug/logs/screencap/runtime，最后写 `update-manifest.json`，任一复制失败均不启动或宣称新版本。
- 发行用 `MFAUpdater.exe` 启用安全裁剪并保持单文件自包含，避免为了退出后覆盖逻辑额外携带整套未裁剪 .NET 运行时。
- 同时发布协力慢准备恢复：入房等待 180 秒与点击“不指定歌曲”后的 60 秒拆分计时，漏检短黑场时只接受连续稳定且身份完整确认的最终封面；已经进入歌曲中段时保持 fail-closed，不启动演奏引擎。
- 自动化和本地升级演练完成后发布正式版本；v1.3.5 → v1.3.6 的公开 GitHub 更新链路由用户发布后亲自验收。

## 2026-09-10（协力慢准备恢复，待真机验收）

- 协力入房后的准备等待拆成两个独立阶段：最多 180 秒等待“不指定歌曲”或直接出现准备页，超时后退到桌面、重新切回游戏并结束任务；点击“不指定歌曲”后重新计时，独立等待准备页 60 秒，不再消耗前一阶段的剩余额度。
- 协力点击“准备完毕”后若漏检短黑场，允许连续两帧稳定且由本局难度、等级、标题共同确认的最终歌曲封面作为正常转场证据；封面帧和解析结果直接传给演奏入口，不再重新等待黑场或重复解析。任意高纹理加载画面、静态准备页和动态演奏中段仍不得放行。
- 协力准备完毕后的开演观察窗由 12 秒扩至 60 秒，覆盖烧条倒计时和成员准备较慢的场景；若动态演奏场证明黑场已漏检，则只继续数值生命监控，确认生命归零或监控超时后退到桌面再切回游戏并停止任务，绝不从歌曲中段启动演奏引擎。

## 2026-09-10（v1.3.5）

- 协力准备完毕后高频确认按钮送达、成员退出与真实黑场；漏掉转场时拒绝从歌曲中段启动演奏，并补全低血与生命归零证据。
- Native 取消会话必须等到本轮设备端实际执行 `r` 才确认触点释放，避免空血停止后遗留触点；生命归零的预期取消通过严格完整性门禁后交给外层手动跳车流程，Maa 不自动修改模拟器网络。
- Native 首切片新增 host 准备、socket publish、设备接收/首执行及切片内部 wait 的分段诊断，不改变调度、锚点、Profile offset 或 timing correction。
- Legacy HOLD/Slide 开局锁相只消费离散拓扑事件，并新增单节点最大绝对残差 25ms 门禁，拒绝含 98.46ms 离群节点的假相位候选。
- 真机验收：雷电协力空血局确认 `reset_executed=true`、`release_confirmed=true`、无强制 kill 或传输错误，并正常进入手动跳车提示；Legacy 锁相修复仍只有离线 replay 与自动化验证。

## 2026-09-09（开发流程：精简专用 Agent）

- 删除职责重叠且会强制重复消耗额度的 Sol `mbd_timing_reviewer` 与 `mbd_core_implementer`。保留 Luna 的日志/trace 扫描和 Terra 的 replay/实现角色；复杂 timing 的根因审查、反例检查和修改后复核改由主 Agent 负责，核心代码统一交给 `mbd_implementer` 单写入，验证与真机门禁不变。

## 2026-09-09（Native 触点释放与空血跳车门禁）

- Native 清理不再把 reset 写入 socket 或设备进程退出当作触点已释放：本轮请求前固定 jlog 游标，只接受游标后的精确 `command == "r"` 作为设备执行证据；旧回执不可复用。已可能发布 DOWN 的会话最长等待 750ms 队列和 250ms 清理，超时仍强制关闭但 `release_confirmed=false`。结果新增 `reset_requested`、`reset_sent`、`reset_executed`、`reset_execution_latency_ms`、`release_proof`、`forced_kill_used`。
- 协力生命归零引发的 Native 取消只有在 backend/session 均为 cancelled、reset 与释放均确认、无队列下溢及传输/设备/观察错误、动作计数有序且 1 秒内停止时，才作为预期跳车终态交给外层手动跳车提示；其他取消继续 fail-closed。
- Legacy HOLD/Slide bootstrap 候选增加独立的单节点最大绝对残差 25ms 硬门禁。真实 `Light a fire` 失败 trace 中 MAD 仅 2.6ms、但含 98.46ms 离群节点的假锁相现在会被拒绝；候选排名、anchor、scheduler、Profile offset 和 FAST/SLOW feedback 未改变。

## 2026-09-09（协力准备后漏黑场与生命归零证据）

- 协力点击“准备完毕”后不再固定等待 2 秒：高频观察按钮送达、成员退出和黑场，并把已观察到的黑场交给后续流程。漏黑场时，完整演奏场加连续局部音符运动只作为“歌曲已开始”的 fail-closed 证据，拒绝大面积转场，绝不在中段启动 Native/Legacy；超时同样 fail-closed。日志记录 `outcome`、耗时和 timeout，成员退出原有处理不变。
- 调试录像/trace 开启数值生命监控时，结果 JSON 增加 `life_monitor_diagnostics`：可见/不可见样本、最低生命、`<20` 候选及最大连续帧、最终 zero streak、alive/dead 确认状态和未触发归零原因。首个 `<20` 候选由 recorder 工作线程保存一张事件截图，不在实时热路径同步写盘；生命阈值、归零回调和断网跳车行为不变。

## 2026-09-09（Native 首切片提交链诊断）

- Native 结果新增每局首切片一次性的结构化时间线，记录 PlaybackSession 取窗、Python 编译/映射/回执/载荷构造、socket publish、首条设备 jlog 及首个 `w N` 的请求/实耗/误差边界；缺失证据保持 `null`，宿主与设备单调时钟域及正负语义显式标注。
- Minitouch 传输只读统计最近一次 publish 的 payload 字节、`send()` 调用次数、实际发送字节与成功状态，用于区分宿主构造、socket 背压和设备 reader 唤醒延迟；未修改 anchor、窗口、等待、队列、Profile offset 或 timing correction。

## 2026-09-09（开发流程：常规与核心实现 Agent 分级）

- 用户级 `mbd_implementer` 改由 Terra High 承担根因明确的常规代码、测试、诊断、Legacy、replay、导航和普通 realtime 修改；新增 Sol High 的 `mbd_core_implementer`，仅在 `mbd_timing_reviewer` 完成独立根因审查并放行后修改核心时间域、Native scheduler/minitouch、ChartPredictor 核心锁相、Profile timing 语义或 Native/Legacy 共用链路。
- 同一批文件只允许主 Agent、常规实现 Agent 或核心实现 Agent 中的一方修改；核心修改保持“审查 → 实现 → 测试/replay → 修改后复核 → 用户真机验收”的门禁。

## 2026-09-09（Legacy HOLD/Slide 开局谱面对齐候选，待真机验收）

- Legacy 谱面辅助的 bootstrap alignment 新增离散 HOLD/Slide 证据：按 50ms 聚合同一组视觉 DOWN 轨道，并匹配已确认本地谱面的 Slide 头；同一触点的离散换轨可继续匹配连接点拓扑。连续绿色像素和逐帧 HOLD 不计入锁相样本。
- 锁相至少要求 4 个谱面节点和 2 组离散事件；只有一组绿条、重复轨道图样存在多个相位、残差 MAD 超限或 confidence 不足时继续原视觉路径。既有 TAP/FLICK/SKILL 投影校准、锁定后谱面执行和 fail-closed 回退保持不变。
- `Light a fire` Expert 两份 2026-09-08 记录离线重放均在第二组 Slide 阶段锁相：单人由约 19.001s 提前到约 5.318s；协力原记录未锁相，新重放约 9.768s 锁相。该结果仅为离线候选，尚未完成真机验收。

## 2026-09-08（v1.3.4）

- 更新公告：启动客户端自动检查更新，并在主页弹出最新 Release 的简介（Markdown + emoji 渲染），每个新 tag 只展示一次；标题栏显示“发现新版本 vX.Y.Z”按钮，点击直接整包更新。
- 自动更新：新增“自动检测更新 / 自动更新资源”两个开关；开启后启动时静默检查，发现新版本且应用空闲时自动下载整包并在退出后应用重启；下载期间若用户开始任务则不强制退出，等任务结束后退出时再应用。
- 每次更新后安装目录名自动跟随版本变化（`MaaBanGDream-v1.3.3-win-x64` → `MaaBanGDream-v1.3.4-win-x64`）：启动器在启动 MFA 前检测目录名与清单版本不一致时，用脱离启动器的辅助进程改名并重启，不改用户自定义目录名。
- 新增发布说明模板 `docs/release-notes-template.md`，Release 简介统一采用 emoji 分区格式，与主页公告共用同一份 Markdown。
- 更新包不再携带 354MB 的 Python 运行库归档（首启解压后即删除）与本地谱面库 `resource/charts`（谱面走“演出设置 → 谱面辅助 → 同步”独立通道）；更新包从约 369MB 降到约 143MB，仅在本机运行库缺失时才回退下载完整包。

## 2026-09-08（v1.3.3）

- 更新机制改为 MAA 生态一致的纯 GitHub 整包更新：不再对发布 zip 做逐文件 Range 增量（那套会重复拉 354MB 的 Python 运行时 zip、在国内网络卡住且无法续传）。现在整包下载到 `.part`（curl / HttpClient Range 断点续传），下载后与发布附带的 `.zip.sha256` 校验一致，再由重启辅助脚本在进程退出后覆盖解压（保留 config/profiles/logs 等用户数据）。
- 本地版本判断以 `update-manifest.json` 为准（只在一次完整应用成功后才写入），中断的半更新状态不会再假显示成新版本、也不会挡住补完更新；启动器 `scripts/update.ps1` 与 MFA 内置更新器使用同一套规则，旧的 8 bit 复核等无关逻辑不受影响。
- 更新设置页的“检查资源 / 资源更新 / 重新下载资源全量包”全部接到 GitHub 整包更新；“自动更新资源”在应用空闲且发现新版本时自动整包更新并重启。

## 2026-09-08（v1.3.2）

- 协力谱面相位校准候选窗在协力模式放宽到 60 秒，并在锁定后加“相位不可验证”fail-closed 兜底，修复等待成员过久时第二局锁到假相位、开局几个键后整段盲压到生命归零的问题；真机 Legacy 协力连续 8+ 局可靠。
- 最终封面门控改用与谱面仓库一致的“等级硬约束 + 14 bit”阈值，并接入开演前歌曲信息页封面下方的标题行 OCR 作为谱面解析辅助证据，修复 Little Busters! 等封面位距 10~14 bit 的歌曲被 8 bit 复核拒绝、Native 被静默降级成视觉 Legacy 的问题；真机 Native 协力验收通过。
- 启动器改为纯 ASCII（修复 zh-CN 系统首次启动时 UTF-8 注释被按 GBK 误解码执行、报“启动前静默检查 GitHub 不是内部或外部命令”的问题）；定制 MFA 演出设置页统一 NOTE/TAP 特效命名、精简 Profile 时间列并新增特效/引擎列、把校准流速并入演出特效页、钉选按所选文件自身的难度。
- 断网跳车 UID 解析回退 `pidof`、跳车失败清理回主页并结束任务；结算退出确认改点取消、回主页多按 ESC 的退出弹窗处理；Profile 环境签名纳入 native/legacy 引擎。
- 验证 963 passed / 7 skipped；发布包重新打包并通过干净解压首次启动验证。

## 2026-09-08（协力谱面相位校准：等待成员过久不再锁错谱面时钟）

- 协力局演奏场出现后，歌曲要等“其他成员准备中”结束才开始；等待超过约 12 秒时，校准候选窗会把真实相位排除，周期性段落里的假相位（实测 `FIRE BIRD` 锁到 -4522ms，比真实 -13530ms 早约 8.9 秒）接管谱面时钟，开局几个视觉按键后整段盲压、生命归零。
- 协力模式把校准候选窗下限放宽到 60 秒（其余模式保持 12 秒）；离线重放 2026-09-08 失败录像确认从假相位 -4522ms 改锁真实相位 -13530ms，预测按压全部落在同 lane 视觉音符上；正常局（-3033ms）不受影响。
- 新增锁定后“相位不可验证”fail-closed：滑动窗口 24 个可信投影中 ≥65% 在同 lane ±350ms 内找不到任何谱面判定时，放弃 chart 输入回退纯视觉并记录原因，防止候选窗仍然不够宽（等待超过 60 秒）或歌曲身份错误时继续盲压。

## 2026-09-08（协力最终封面 8 bit 复核过严：Native 被错误降级）

- `Little Busters!`（bestdori 46，Expert 25）最终封面实测 pHash 稳定在 10 bit：`LocalChartRepository.resolve` 已按“14 bit + 等级硬约束”正确解析出谱面，但 `FinalCoverGate` 仍用 8 bit 严格复核同一张封面，把刚解析出的谱面重新拒绝，最终 `degraded-visual-legacy`，Native 拿不到谱面、视觉 Legacy 打空血（2026-09-08 14:39 录像）。
- 封面门控在与等级硬约束一致时改用与仓库相同的 14 bit 宽阈值，不再出现“仓库宽阈值解析、门控 8 bit 复核否决”的不一致；离线用该局真实封面帧验证：`c7b9cb102fcfb04a`（距 46 的 `c7bac9172dceb062` 10 bit）现确认 Little Busters! 并选中 `bestdori/46/expert.json`。
- 开演前最终歌曲信息页封面下方的标题行（ROI `(480,485,318,70)`，实测 Little Busters! 置信度 0.93+）接入谱面解析：协力准备页标题常读乱，最终页标题在延迟解析路径里刷新 resolver 的标题证据，用于指纹歧义时按“标题 + 等级”唯一化。只有该读数能在当前难度等级下唯一匹配本地曲目时才替换，加载页的“目标得分”等无关文字一律忽略，不覆盖准备页已可靠的标题。

## 2026-09-08（断网跳车：UID 解析回退 + 失败后清理回主页）

- `resolve_game_uid` 在 `dumpsys package` 失败/超时时回退到 `pidof` + `/proc/<pid>/status`，避免演奏中因“无法解析游戏 UID”导致门禁 fail-closed。
- 跳车失败（断网门禁/弹窗流程未成功）时不再跳过结算导航假装完成，改为清理回主页/房间选择并继续下一局，本局仍按完成计入，避免任务在演奏场反复按返回卡住不结束。

## 2026-09-07（v1.3.1）

- 协力开局前预检 Profile 环境：签名不匹配（例如任务执行中改过 TAP EFFECT）在任务一开始就报出具体不一致字段并失败，不再进房到准备页才被拒，也不再空转重试；报错点名具体 Profile 文件。
- 断网跳车在非 root shell 下用 `su -c` 提权执行 iptables，修复雷电上 `gate_block_failed`（真机验证 REJECT/恢复通过）。
- 定制 MFA 演出设置页“钉选当前”改为按所选文件自身的校准难度钉选，难度下拉框只负责筛选列表；两个仓库改为 merge 提交合入 `main` 并清理已发布残留分支。
- 待真机验收：断网跳车完整两段弹窗流程、钉选交互、协力 Legacy 在雷电上的时序锚定（已知会提前约 4 秒导致 14 秒打空血，雷电协力请用 Native）。

## 2026-09-07（Profile 环境拒绝原因诊断）

- Profile 环境签名不匹配时，拒绝信息现在列出具体不一致字段（例如 `TAP EFFECT 1 ≠ 4`）；钉选与自动选择两条“非流速环境”路径都覆盖，不再只报笼统的“与当前非流速环境不匹配”。

## 2026-09-07（协力 Profile 校验提前到任务开始）

- 协力把 Profile 环境解析从“准备页流速门禁”提前到任务一开始：签名不匹配（例如任务执行过程中手动改过 TAP EFFECT）时立即作为任务失败返回并写入具体原因，不再先导航进房间、在准备页才被拒，也不再空转 `play_failure_retry_count` 次重试；截图不可用时回退到准备页的既有门禁。

## 2026-09-07（协力断网跳车：非 root shell 提权）

- 修复生命归零跳车在雷电上 `gate_block_failed`：adb shell 默认是 uid 2000，`iptables` 会报 Permission denied；`GameNetworkGate` 现在先探测 `id -u`，非 root 时用 `su -c "<命令>"` 提权，并在失败时携带可读原因（fail-closed 语义不变）。已在雷电真机验证：非 root shell 下 REJECT/恢复端到端通过。

## 2026-09-07（仓库门禁修订：分支清理与 merge 策略）

- 合并策略由 Squash 改为 Merge（`--no-ff`，创建合并提交）：`main` 保留每个 feature/fix 分支的完整提交历史，发布变更可直接由 `git log` 范围得到，不再依赖手工整理的交接记录。
- 清理 22 个已合并/已发布的本地残留 feature、fix、docs 分支（均有已合并 PR，或为已合并分支的祖先；`fix/realtime-calibration-recovery` 的 PR #20 已关闭且两条改动各自被 revert）。保留 `main`、`release/v1.2.1`、`backup/local-main-before-v0.8`、`codex/release-v0.8.0`。
- AGENTS.md：删除过时的“统一开发分支 `feature/cooperative-safety`”，改为“从 `main` 拉分支、验收后 merge 回 `main`、部署从 `main` 进行、合并后删分支”；发布门禁与禁止提交清单不变。

## 2026-09-07（v1.3.0，已发布）

- **Native 漂移定案**：同一份代码在雷电模拟器 Native `[FULL]FIRE BIRD` 2331 PERFECT/0 GREAT（漂移 p50 5.2ms），MuMu 漂移按 20 秒分段 1.09→1.47→4.89→5.64ms/s 加速增长、逐局 p50 146.8/108/54/88ms 波动，高性能模式与关闭主机负载均无效；速率校正实验闭环在 MuMu 发散，保持默认关闭。分工：雷电走 Native、MuMu 走 Legacy（MuMu 新号 Legacy 490P/17G/1M，hit 99.8%）。有符号漂移证据新增按 chunk 中位数估计速率并闭环补偿。
- **FULL 谱面身份**：准备页（带演出开始按钮的页面）左下角读标题/等级/难度；最终封面右上角新增 FULL 徽标检测作为标题 OCR 失败的补充证据；仅同时读到等级时放宽 pHash 阈值到 14 bit。`[FULL]FIRE BIRD` 真机 2326P/1G/4M。
- **成员退出弹窗**：重新提取当前“错误”标题模板（锚点 399,158、确定 638,525）；新增 `watch_member_exit_before_black()` 覆盖“点准备完毕后、黑场转场前”的弹窗窗口，防止弹窗挡住转场卡死整局。
- **协力断网跳车**：完整链路——按游戏 UID 的 iptables 门禁（雷电实测可用，MuMu 缺 `xt_owner` 且单网卡、断网必杀 adb，暂 fail-closed）、真实两段弹窗模板（“通信已中断。是否继续演出？”左“中断” 508,447；“确认中断当前演出返回主页吗？”右“中断” 754,439）、连接失败有界重试、引擎生命归零钩子与 UI“断网跳车”选项。
- 协力最终封面退化新增指纹诊断日志（每个新指纹一条），便于定位“封面可见但不认识”的歌曲/位距。
- 更新项目提示词：Profile offset 误诊教训、MuMu 时钟偏斜与 adb 端口影子、MFA 渲染器崩溃等坑位。
- 完整验证 948 passed / 7 skipped；不创建 tag 或 Release。

## 2026-09-06（v1.2.4-dev 本地测试候选，未发布）

- Native 结果 JSON 新增 `execution_timing`：保存动作 token、chunk、相对首拍的计划时间、计划/实际执行时间、有符号漂移及回读时间；完整 chunk 汇总中位数，未完成 chunk 保留已收到的动作证据。内存采集不改变时钟、调度或补偿，真机漂移原因仍待取证。

- Native 编译器和 Agent 新增默认关闭的时钟速率校正实验（`MAABANGDREAM_NATIVE_DRIFT_RATE_CORRECTION=1`）：按最近有符号漂移估计宿主↔设备时钟速率偏斜，编译器把每个 `w` 按 `(1-rate)` 换算成设备时间。与逐命令成本校准正交，不向残差通道叠加补偿；仍待多局真机验收。

- 单人实时演奏的排练模式恢复使用已验收 Profile（`require_profile=true` 且 `rehearsal_mode=true`），开场即沿用 Profile 的 timing offset，不再从 0 自校准；只有“实时演奏校准”任务的排练阶段才关闭 Profile 要求。

- 调试录像目录名增加演奏类型前缀（`coop-*`、`single-formal-*`、`single-rehearsal-*`、`challenge-*`、`calibration-*`、`continuous-*`、`visual-eval-*`），便于直接区分录像来源。

- 合并 v1.2.3：便携包 BOM/Unblock 修复、内置 Native 扩展、无本地谱面回退 Legacy、minitouch 竖屏物理面旋转与内置默认校准 Profile。
- 汇集结算剧情跳过、Native commit 间隔补偿、准备页身份复核、ordered startup 门控、按实际 `w` 命令数结算等待成本、按底层 Tasker 句柄保存重试预算等候选。
- 修复中文/非 ASCII 安装路径（如 `D:\下载\`）下便携包无法开演：cv2 图像读写改为字节级 `imdecode`/`imencode`，Native 谱面读取在 Windows 改用 UTF-16 `_wfopen`。
- 修复校准“当前曲目”模式把同一封面的 pHash 采样噪声误判成换歌：身份比较复用 `same_song` 的 Hamming 距离容忍，同一首歌两次采样的 2 bit 差异不再导致正式验证被拒。
- 修复协力结算导航在停止检查阻塞越过等待窗口时 `time.sleep` 负值崩溃：等待循环先取剩余时间并钳制非负，崩溃不再把“结算页未识别”升级成整局异常；主页恢复重启前保存最后一帧证据截图。
- 完整验证 908 passed / 7 skipped，Native 构建 1786 项 C++ 检查通过；仅供真机验收，不创建 tag 或 Release。

## 2026-09-05（结算与时序修复候选，待真机验收）

- 修复实际选择难度后准备页身份门禁失效：MaaFramework 整块替换 `custom_action_param`，五档难度的 interface override 需显式保留标题延后和准备页复核参数。真实框架覆盖回归修复前五档均失败，修复后均通过。
- 单人歌曲身份在点击演出开始前的选择乐队页复核：读取左下角标题、难度和等级，再预武装；选曲列表不再读取标题。真实 FULL FIRE BIRD 截图离线确认 Expert 28 / Bestdori 243，普通版为 187 / Expert 27。小字号 8 的高质量绝对匹配不再被过严的 6/8 距离比否决。
- 明确 FULL 前缀约束版本候选，仍拒绝等级冲突；标题噪声裁剪长度受限，防止把 `ERER` 截成《R》。身份读取优先保留等级证据，等级缺失时有界重读且不重复点击难度。
- 单人重试按底层 Tasker 句柄保存预算，不再用每次回调新建的 Python 包装对象地址；预算耗尽后保持拒绝，只有新局 reset 才清零。设置重试 1 次即最多 2 次尝试。
- 准备页身份候选随 `-OrderedStartupTrial` 显式启用；完整验证为 895 passed / 7 skipped，真实准备页离线匹配通过，长曲与重试行为仍待用户真机验收。
- Native 等待补偿按实际发出的 `w` 命令数结算；舍入为零的亚毫秒等待归还预估成本，拆分长等待补齐额外成本。最后一首六兆年的真实按下回执中位偏差从前段约 +7 ms 变为末段约 -57 ms；固定等待开销的原谱面模型末尾误差由 -16.3 ms 降至 -0.23 ms，高层 2387 动作/739 按下保持一致。模型不等于实机成绩，原有时延 Profile 未修改。

- 新增默认关闭的 `-OrderedStartupTrial` 本地启动候选：准备页不能启动封面/演奏场判定，必须先观察全黑开演转场；预检查已捕获的黑帧也交给门控，避免诊断截图消耗唯一黑帧。
- Legacy 复用完整演奏场、协力等待弹窗与首音门控，首音前禁止音符派发和结算；生命保护开启与关闭均受启动门控约束，Native 时钟补偿不变。调试包记录等待黑场、黑场转场及首音阶段。
- 最新失败截图离线重放：旧逻辑在准备页两帧后降级并开演，实际开演黑场被误记为结算；候选在准备页等待黑场，在黑场等待封面，不进入输入或结算。原始音符 trace 重放保持 1 次动作，说明单独重放热路径不能验证此启动故障。
- 开演候选完整验证（开启试验环境）：`verify.ps1` 为 883 passed / 7 skipped，真机验收尚未完成。

- 单人/协力通用结算及协力回房间流程接入已有剧情菜单、跳过与确认模板；每次点击后重新截图，不再用 BACK 取消剧情确认框。
- 协力结算每帧同时检查房间、主页与剧情，不再在每次 BACK 前后分别等待两秒；使用独立的低延迟截图节点，保持 Maa 安全刷新方式及停止检查。
- Native 编译器补齐 commit 前的命令间隔补偿。恒定设备间隔的确定性用例在旧实现中随音符数漂移，修复后通过；已有 Native 默认关闭开关及 Profile 偏移保持原值，不宣称已解决全部实机波动。
- Native 终态日志单独输出时序门禁、漂移及测量不确定度，不将输入完整性通过等同于游戏判定达标。
- 本候选只供本地测试；实机成绩、结算对话推进及手动停止行为由用户验收，未推送或发布。
- 离线验证：`verify.ps1` 为 876 passed / 7 skipped；Native 构建与 1786 项 C++ 检查通过；部署后的完整运行时兼容检查通过。EXIST 原谱面新旧高层动作一致，固定每命令 0.01 ms 开销的模型误差上限由约 22.5 ms 降至 1.3 ms；该模型不代表实机调度或游戏判定。

## 2026-09-05（v1.2.3）

- Native 已启用但没有可靠本地谱面时（例如 Easy/Normal 未收录，或等级冲突）不再在预武装阶段失败：预武装跳过并整局回退 Legacy 视觉演奏，选完难度后正常点排练/开始。
- `prepare_native_for_settings_gate` 无谱面时返回 None 而不是抛错；`RealtimeProfilePlay` 在开演前把 Native 关成 Legacy，禁止中途混用。

## 2026-09-05（v1.2.2）

- 修复便携包缺失 `maabangdream_realtime.pyd`：发布构建现在显式调用 `build_native_realtime.ps1` 构建 Native 扩展并内置到 `agent/realtime/native/`。
- 发布包校验增加 Native 扩展存在性与导入/版本自检，防止再次出现“打开 Native 后整局零输入”。
- 开演前门禁失败提示不再统一说成“流速验证失败”，改为指出可能是流速设置或 Native 预武装失败。

## 2026-09-05（v1.2.1）

- 修复便携包二次启动失败：`check_runtime.py` 用 `utf-8-sig` 读取 JSON，容忍 Windows PowerShell 5.1 生成的 `interface.json` BOM。
- `start-release.ps1` 与 `launch-mfa.ps1` 生成 JSON 一律无 BOM 写入。
- `start-release.ps1` 启动前对 `MFAAvalonia.exe` 执行 `Unblock-File`，解除浏览器下载压缩包带来的 Zone.Identifier，避免 SmartScreen 取消导致 Start-Process 报 “The operation was canceled by the user”。

## 2026-09-05（v1.2.0）

- 协力首拍门控拦截“其他成员正在准备中”弹窗（含缩放出现/消失与背景变暗）：弹窗存在/消失只重置基线，大面积整行变化不再被当成首音；单人/校准/挑战不受影响。
- 协力漏键抖动改为固定漏歌曲末尾的 1~2 个普通单点（漏 1 个还是 2 个由 run_id 决定），并修复 jittered 副本与最终封面谱面按路径判等导致的预武装消费失败，改为按歌曲身份判等。
- 标题 OCR 匹配容忍首尾噪声（例如省略号被读成“今の”），准备页谱面不再因尾部杂字整局回退。
- 最终封面门控识别整屏黑场转场，黑场后进行一段无 sleep 的密集采样，给短暂出现的封面留出匹配窗口，避免黑场/淡入帧被误当成“没有封面”。
- 修复 `game_effect_settings_enabled=false` 跳过演出设置页时漏掉单人 Native 预武装、导致整局零输入的回归；跳过时仍按 deferred 规则预武装或推迟。
- Legacy 视觉演奏抑制 hold 起手后同轨道头碎片产生的二次 TAP，避免一颗长条被按两次。
- 协力结算后识别不到房间页不再终止任务：先继续推进结算，仍失败恢复主页（允许重启游戏）继续下一局；最后一局 stay 失败按完成返回；结算导航不再识别成员退出弹窗。
- `AGENTS.md` 新增“演出类型流程”，记录各演出任务与 `RealtimeProfilePlay` 通用单局流程。

## 2026-09-05（v1.2.0 验证状态）

- 自动化验证：`verify.ps1` 全部通过，含上述回归的单元测试与真实 `trace.jsonl` 离线重放。
- 仍需真机复验：协力准备弹窗门控、末尾漏键、结算回不去房间后的恢复、单人跳过设置页后的 Native 预武装；本版发布不宣称这些场景已真机验收。

## 2026-09-04（v1.1.0）

- 最终封面从硬失败改为分层降级：准备页已有可信谱面时保留原谱面继续；协力仍无法解析谱面时，在发送任何触控前关闭本局 Native 和谱面派发，整局使用 Legacy 视觉演奏。准备页难度、等级和共享封面标题的真实冲突继续硬拒绝。
- 实时 Trace/录像提前到最终封面门控之前，新增封面开始/终态、引擎终态和结算关键截图及结构化阶段索引；即使只启用轻度 Trace，门控失败也会保留现场，不再只留下文字错误。
- 新增 `play_failure_retry_count` 运行设置，范围 0–3、默认 1；普通单人、校准在同一阶段技术失败时重新执行完整单局，协力在重试前释放 Native 并恢复主页后重新入房。用户停止、身份/配置硬冲突和成员退出继续走各自策略，不会被普通技术重试吞掉；挑战演出因会消耗挑战次数不自动重试。
- 定制 MFA“演出设置 → 任务安全”新增“单局技术失败重试次数”，与 Agent 的运行设置原子保存。
- 生命归零“演出失败”弹窗加入约 5 Hz 的终态监控：确认弹窗后引擎以“演出失败：生命值归零”干净收尾，再按“演出失败弹窗 → 退出确认 → 主页”有界退出，绝不点击需要消耗星石的“继续”；`CommonRecover` 同步接入该链路，彻底消除 ESC 在弹窗与确认框之间来回的死循环。Native 完整性门禁不再把生命归零这种游戏终态误报成技术失败。
- 校准改为从已钉选/已验收 Profile 的时序偏移热启动，并按 FAST/SLOW 占比自适应大步长（最大 ±48 ms），一轮排练即可追平 30–50 ms 的会话延迟，避免正式验证带着旧偏移直接死亡；生命归零的正式验证按确定失败结束会话，不再盲目重试。
- 用户停止校准不再误报“✅ 实时演奏校准成功”：成功提示只在正式验证真正通过、Profile 被接受时输出，停止时任务显示“已放弃本次任务”。
- 定制 MFA“演出设置 → 实时演奏 Profile”列表支持调整列宽，创建/验收/修改时间完整显示，不再被省略号截断。
- 任务失败日志去重：移除各演奏节点与失败节点重复的失败提示，只保留 TaskOutcome 带具体原因的一条；MFA 不再把含“/”的普通文本当成文件路径而刷“Focus 内容文件解析失败”告警。

## 2026-09-04（v1.1.0 真机验收）

- 校准真机验收：从 66 ms 热启动，排练（Bestdori 430）838 PERFECT / 26 GREAT / 2 MISS，自动建议 78 ms；正式验证（Bestdori 467）881 PERFECT / 13 GREAT / 1 MISS 存活并通过，新 Profile `expert-20260904015731.json`（offset=78 ms）被接受。
- 受控死亡测试：以 -120 ms 错误偏移跑正式局，引擎正确识别 0/1000 失败弹窗并退出回主页，失败只报一条“任务失败，已完成 0/1：演出失败：生命值归零”，无重试、无重复红叉。
- 连续随机正式演奏稳定性：66 ms 四局（698/4、597/3、665/0、551/90）与 78 ms 四局（641/1、863/0、868/0、896/21）共八局全部打完结算，均零 MISS、零 GOOD/BAD、零下溢、零重试，设备派发漂移 P95 稳定在约 32–56 ms；78 ms 明显优于 66 ms，已钉选为 Expert 默认。
- 注意：Native V2“连续 10 局每局 GREAT ≤ 5 且无 GOOD/BAD/MISS”的正式里程碑仍未达成（部分歌曲仍有 21–90 个 GREAT），本版不据此宣称稳定 AP。

## 2026-09-03（v1.1.0 开发记录）

- 单人、校准、挑战和协力演出统一增加最终封面门控：准备页先确认难度、等级和本地谱面，点击开始后再用居中封面复核实际进入的歌曲；共享封面仍强制结合本地标题 OCR 消歧，封面只负责收窄候选，首音继续单独建立歌曲时钟。
- 协力流程不再等到演奏场出现后才启动 `RealtimeProfilePlay`，Ready 后立即观察可变网络加载和最终封面；Native 协力预武装延后到最终封面确认，超时、错谱或已进入演奏场仍未确认时失败关闭，禁止漏首音后切回 Legacy。
- “生命保护”关闭时不再构造或逐帧调用数值 `LifeDetector`；Legacy 用生命条控件存在性与六轨判定标记完成启动门控，开演后降为约 5 Hz 的演奏场/结算转场监控，结算动画不再进入音符检测。生命保护开启时保留原数值阈值、骤降恢复和暂停策略。
- Native 演奏会话改为 `prearm -> 光闸启动 -> 滚动发布 -> drain/finish 或 cancel -> report`：开始前完成谱面编译和武装，C++ 以 500 ms 前瞻、200 ms 低水位和 750 ms 队列上限持续发布短块，不再整曲一次性塞入设备队列。
- Python 开演后只保留约 5 Hz 的生命与终态监控以及结果解析，不再逐音符调度；Native 从首个音符起独占输入，故障时安全终止，禁止同局混合输入或中途切回 Legacy。
- 修正 HOLD 最终尾点、尾部 FLICK 同触点 `MOVE -> UP` 和和弦统一 commit；加入触点生命周期校验，所有正常、停止和异常终态都释放活跃触点。
- minitouch jlog 改为异步完整命令 FIFO 校验，并把动作来源命令关联到实际 commit 回执；主机/设备时钟映射记录探测中点和不确定度，只用已执行块校正尚未发送的未来块。
- 触控脚本统一采用纳秒时间键，修复长时间运行后理论同刻的 UP/DOWN 因浮点 ULP 倒序；滚动块会预留窗口尾后即将开始的 HOLD 触点，避免跨块 FLICK 抢占；同时修复整数毫秒等待被重复补偿而使长曲相位随机游走的问题。
- Native 报告统一关联 run ID、原始 jlog 和游戏判定，并输出计划/发送/执行数、手势与和弦、分块下溢、队列深度、时钟偏移、不确定度、漂移分位数、停止延迟和明确终止原因；证据不完整或计数不一致时拒绝正常完成。
- Native 完整性门禁进一步要求后端与 C++ 会话都以 `finished` 结束，且 500 ms 内取得设备端 minitouch PID 退出证据；准备超时会取消启动，迟到线程只能自清理，禁止 stop 后重新连接或发布探测命令。无 ACK 的 reset 写入不再被误记为释放成功。
- 修复 Native 首音门控把上游无帧率上限的“稳定 200 帧”直接移植到 60 FPS 循环所造成的秒级延迟：改为连续稳定时长、绝对颜色变化、受干扰即重置和 500 ms 前奏残留宽限，并插值阈值穿越时刻。协力已确认生命条出现，使用更短的基线窗口；单人、校准与挑战使用开始过渡窗口，但二者不引入不同的歌曲时间偏移。每局报告新增实际等待、稳定耗时、忽略事件、触发分数及有界关键事件。
- 修复 Native 将视觉预判线 `y=565` 误作设备触控落点的问题；TAP、FLICK、HOLD/Slide 现在与 Legacy 和上游手势一致落在判定线 `y=590`，运行报告新增 `touch_y` 供真机验收。
- 使用用户从主页到正式演出的 34 秒录像复现 Native 首音误触发：旧门控在生命条尚未出现时把全黑加载转场当成首音，导致同曲首批输入约提前 6.7 秒；只增加生命条前置条件仍会被后续演奏场淡入误触发。门控现必须同时确认生命条与至少六轨白色判定标记并重置加载页颜色基线，之后才允许稳定窗口、500 ms 宽限和首音检测带触发；报告新增演奏场证据、等待帧数、等待时长与丢失次数。
- 以 8 段速度 5.0 的 60 FPS 真机录像和逐帧时间戳对齐首音门控，其中基准局为 771 PERFECT / 1 GREAT / 0 MISS；沿用上游 30 ms 补偿会在应用同一 Profile 偏移后比已验收 Legacy 首次触控提前约 151–166 ms（排除一段旧 150 ms 偏移异常样本）。Native 速度 5.0 基线改为 190 ms，并在每局报告中显式输出 `photogate_latency_ms`；单人与协力不使用不同歌曲偏移。
- 增加独立虚拟 minitouch 压力测试，离线门槛为闲置及 CPU 压力各 120 秒、零下溢、绝对时延 P95 不超过 3 ms、最大值不超过 8 ms、取消释放不超过 500 ms。虚拟测试不替代 Android 真机；首个真机门槛仍为冻结配置后连续 10 局 Expert 每局 Great 不超过 5 且无 Good/Bad/Miss。
- 2026-09-03 本机虚拟门禁已完成：空闲/压力各 120 秒均为 399 块、2944/2944/2944 动作、零下溢和零协议错误；P95 分别为 0.086/0.052 ms，最大值分别为 0.838/0.440 ms，取消释放约 1.5 ms。该结果只证明主机与虚拟执行器路径，不代表 Android 真机成绩。
- 本节仅记录开发中的本地实现，不表示已经发布或完成真机验收；Native 开关继续默认关闭，Python Legacy 保持可独立选择。

## 2026-09-01（v1.0.0）

- 新增多人协力演出：支持普通四档房、好友邀请和六位私人房间号三种入房方式，五档难度、1–99 局连续执行、成员下载等待、成员退出策略及好友/私人房同房续演。
- 协力准备页固定选择“不指定歌曲”，并复用流速、演出设置和 Profile 门禁；生命耗尽后仍继续演奏，下载超时会主动跳车，成员退出可明确结束或有界重连。
- 重构单人和协力结算导航：生命条消失后使用“右下角动画跳过 → Android BACK → 重新识别 → 再次动画跳过”的有界循环；真正的右下角像素不会误触主页按钮，协力多局回到主页时会显式重新进入协力房间页。
- 任务列表统一增加 Emoji；“实时演奏”更名为“单人实时演奏”，并为“协力演出”增加独立标识。
- 此正式版汇总 v0.9.1 候选版中的便携 Windows x64 完整包、手动流程录像、MuMu 启动兼容、新账号首次演出设置确认、结算加载等待和新版应用图标；相关流程均已完成自动验证，协力连续执行与结算由用户真机验收通过。
- 便携启动器在首次解包后会以便携模式校验 Python、MaaFramework 和定制 MFA 版本；源码环境仍严格要求名为 `maabangdream` 的 Conda 环境，便携包则只接受已完成路径修复的包内 `runtime/python`，避免把目录名差异误报成组件不兼容。

## 2026-08-31（v0.9.1-rc.3）

- MFA 新增通用“手动流程录像”任务，可选择 5/10/15 FPS、5/15/30 分钟和触摸位置显示；停止任务时保存可随机定位的 MKV、逐帧时间索引、首末帧和摘要，供后续活动演出与新流程取样。
- 前台应用识别兼容 MuMu 对 `dumpsys window windows` 返回空结果的情况，依次尝试 Window 与 Activity 状态，并在 Maa Controller 无法解析时使用同一设备配置执行只读 ADB 查询。
- 新账号首次开演出现“进行演出前设置确认”时自动识别并点击“确定”；正常选曲页样本与提示模板保持明显分离，避免误点。
- 点击开始后先等待提示弹窗，并将实时演奏生命条启动门禁由默认 20 秒延长到 60 秒，以兼容协力演出和较慢加载。
- 本候选版本保持 GitHub Pre-release；自动测试、运行时兼容和离线模板门禁通过，首次提示弹窗及后续协力流程仍由用户继续真机验收。

## 2026-08-31（v0.9.1-rc.2）

- 任务栏、窗口和托盘图标改用用户原图的近裁版本，让狐耳、面部、金发和蓝色衣领在 16–32 px 下铺满可用画布；32 px 不透明主体覆盖率由约 61% 提升到约 96%。
- README 和应用大图继续使用用户提供的完整原图；完整相框构图 ICO 另存为 `logo-v2.ico`，更早的旧版 `logo-v1.ico` 也继续保留。

## 2026-08-31（v0.9.1-rc.1）

- 结算采集将歌曲结束后的无标识阶段明确视为 `result-loading`，在 20 秒无输入窗口内等待 PGGBM 数字动画和判定详情标识，避免正常约 5 秒加载过程被误判为未知页并连续发送 BACK。
- 判定详情标识在固定面板区域内兼容动画中的部分标签；真正未知页面仍在等待窗口后执行最多 12 次有界 BACK 恢复。
- 应用、任务栏和 README 更换为新的狐耳相框 Logo；旧 PNG/ICO 以 `-v1` 文件保留，多尺寸任务栏图标提高主体占比。
- 新增 Windows x64 干净发布包：包含自包含定制 MFAAvalonia、MaaFramework、便携 Python 环境、Agent 和本地谱面，无需安装 Miniconda、Python、.NET 或 SDK；打包门禁禁止携带开发者配置、ADB、日志、Profile 和本机绝对路径。
- 本候选版本的离线回归、完整测试和部署验证通过；结算等待修复仍由用户执行最终真机验收，验收前保持 GitHub Pre-release。

## 2026-08-30（v0.9.0）

- 将 Bestdori 全量谱面同步集成到定制 MFA“演出设置 → 谱面辅助”：显示本地清单统计、手动增量同步及逐行进度，已有文件校验后复用，封面按 CN→JP→EN 回退。
- 全量本地仓库只保留 Hard、Expert 和存在的 Special；新增 809 首歌曲的标题、难度等级、封面指纹、内容哈希和预期音符数索引。
- 歌曲确认由“封面唯一命中”升级为封面、标题 OCR 与等级联合约束；共享封面、`[FULL]` 同名版本仍不确定时整局回退视觉，不降低难度。
- 修复难度等级数字 6/8 被过严模板门槛拒绝的问题；共享封面歧义时跨帧重试等级和标题，但不重复点击难度。
- 谱面 Slide 支持严格相邻轨锯齿的临轨判定：满足连续来回切换证据时把触点锚在两轨中点，普通滑条仍按连接点移动。
- 结算页取消奖励、排名、活动和达成报酬坐标点击，统一采用有界 Android BACK；判定详情页优先于相似排名模板，并兼容连续出现的每日/七日奖励弹窗。
- 完善谱面触控、歌曲识别、结果解析、调试记录和运行时设置回归测试。最新完整 Python 验证在提交前重新执行；Normal 共享封面歌曲的真机复验仍待用户完成。

### 2026-08-29（v0.8.2）

- 实时校准重构为可持久化的三次排练加一次正式验证状态机；停止或技术失败后保存会话并从未完成阶段续跑，不再自动追加正式演出。
- 引入本地 Bestdori 谱面仓库、严格歌曲/难度匹配与谱面主时钟；视觉负责锁相、漂移校正和失配回退，谱面负责和弦、密集同轨、FLICK、HOLD 与滑条触控时刻。
- 修复锁相前视觉补点残留、跨帧触点所有权，以及同轨相隔约 118ms 的音符被误判为已覆盖等问题；最终两首 Expert 真机复验均正常完成。
- 结算读取改为页面状态机并增加谱面音符总数校验、页面专用模板和明确技术失败结果，避免错误数字进入校准结论或触发补局。
- 调试记录支持完全关闭、轻度 Trace 与 60 FPS MJPG/MKV；编码和封尾保持后台非阻塞，增加帧映射、丢帧与随机寻址验证，同时兼容历史 AVI。
- 完善游戏资源下载页、演出设置、流速与特效读回门禁，并确保定制 MFA 保存谱面相关开关时不丢失字段。

### 2026-08-24（v0.8.1）

- 实时 TAP 改为跨帧保持 24ms 后异步抬起，避免亚毫秒 DOWN/UP 脉冲被模拟器或游戏间歇吞掉；真实连续演奏已稳定完成 7 局，未再触发生命保护。
- FLICK 的 MOVE/UP 与生命骤降触点释放改为非阻塞派发，移除每 15 秒无条件释放全部触点的干扰，避免 150–265ms 输入等待阻塞截图和音符检测。
- 部署固定使用 `MinitouchAndAdbKey`，并完善触控恢复、输入等待、分阶段帧耗时和明确终态日志，便于区分检测失败、输入静默丢失与模拟器卡顿。
- 收录下列 2026-08-09 开发记录中的视觉设置、结构化终态、调试录制、截图基准和校准修复。

### 2026-08-09（v0.8.1 开发记录）

- 新增单轮 `LiveRunContext` 与无额外截图的 `song-phash-v1` 歌曲指纹；正式结果、校准报告和 Debug Recorder 通过同一 `run_id`/`song_id` 关联，已建立单轮上下文的失败终态输出结构化结果。
- 实时引擎在内存中汇总截图、触控推进、生命检测、检测器、规划器、反馈、录制入队和派发耗时，并记录最多八个大于 100 ms 的长帧及主导阶段。
- 将演出特效门禁扩展为统一视觉设置门禁，支持 TYPE1–7、TAP EFFECT 1–5 和判定辅助的固定模板读回；未复核一致时阻止开演。
- 新增隔离的“视觉设置评估（实验）”模式。实验可复用已验收 Profile 的几何与偏移，但不得接受、覆盖 Profile 或计入正式成功率；ProfileCheck、流速设置、Start 与 Play 均改走五档难度携带完整参数的专用实验节点，避免 MFA 选项按序浅覆盖时丢失难度、流速、600 秒时限及实验标记，普通 Formal 路由保持严格匹配。
- “一键实时演奏”保持纯监听，但只接受最近 15 分钟内的实际视觉设置读回，禁止用 MFA 目标值或无限期旧缓存冒充实测。
- Profile 环境签名加入三项视觉设置；旧 Profile 按 `TYPE1 / TAP EFFECT 1 / 判定辅助开启` 迁移。定制 MFA “演出设置”增加按键类型并自动保存。
- ProfileCheck、流速设置门禁与 ProfilePlay 引擎前准备失败时新增 `preflight_error` 结构化终态，记录 `terminal_stage`、本轮身份、已读回设置与明确原因，消费一次性 Play token，不伪造判定结果，也不创建未实际启动的 Recorder 产物；Challenge、Rehearsal 与实验模式保持各自模式标记。
- 修复 Debug Recorder 立即关闭时清空已接受视频帧的竞态；正常小批量按 FIFO 编码后关闭，超出有界关闭窗口时先写明确的 provisional summary，后台线程退出后再原子刷新最终计数，不向实时线程传播阻塞。
- 只截图观察与离线重放增加结构化截图基准和长帧/丢帧故障注入；新增无损截图后端基准 CLI，默认 dry-run、仅 `--execute` 正式运行、硬拒绝 Minicap，并将逐轮与汇总报告保存到 Git 忽略的 `debug/screencap-benchmarks/`。短 smoke 不得获得正式资格，只有实际覆盖至少 `3×300s` 才可能通过耗时门禁；检测结果和置信度等价仍需另行验证。
- 修复校准在排练轮空转的问题：取消“三首不同歌曲/第四首不同歌曲”的歌曲指纹判重，校准回归延迟校准本身——三轮有效排练逐曲调整延迟后直接进入一轮正式验证；随机选曲仅用于 AutoLive 随机模式，校准不再依赖随机换歌或筛选状态。
- TYPE1–7 视觉读回改为整段 `TYPE<n>` 标签模板匹配，修复 TYPE5 被读成 3、TYPE1 拾取衬线像素导致的“duplicate TYPE rows / readback mismatch”；实机完成 TYPE1–7、TAP1–5、assist on/off 采集，胜出组合为 TYPE1 + TAP4 + assist off。
- 生命保护触发后暂停覆盖层无法确认时不再升级为硬失败：触点已释放，按生命耗尽干净收尾，校准正式轮可以重试而不是整体失败。
- 完成 Debug on/off 正式演奏对照：Debug off 三轮有效 FPS 稳定在 59.8–59.9；Debug on 有两轮降至 51 左右，未满足下降≤3% 门禁，正式演奏建议 Debug off。

### 2026-07-30（v0.8.0）

- 使用《SAVIOR OF SONG》Hard 真机录像修复同一 TAP 被内外圈、重建轮廓或相邻帧重复触发；区分当前轨迹与已消失轨迹，并在动态触发线之外增加真实判定线穿越兜底。
- 游戏内流速每次先进入正确的“演出设定”顶部栏，以固定数字模板读取、按差值修正并复核；模板读数失败或复核不一致时阻止开演。
- 新增可配置并可读回验证的判定辅助效果和 TAP EFFECT 设置；当前实测推荐开启判定辅助并使用 TAP EFFECT 1。
- 登录阶段优先等待右下角菜单标记，再允许通用“点击屏幕任意处”；主页模板统一按真实样本使用 0.82 阈值。
- 调试录像与结算报告新增实际帧率、动作计数、时序反馈及明确终止原因；《SAVIOR OF SONG》Hard 真机结果达到 361 PERFECT、36 GREAT、2 GOOD、1 BAD、1 MISS，有效帧率 58.63。
- 新增两阶段 ALAS 进程互斥门禁：首次发现精确匹配的 AzurLaneAutoScript 进程时安全阻止，用户在同一 MFA 启动会话再次运行时才结束同一组进程；MFA 重启、PID 重用或进程重启都会使授权失效。
- 定制 MFA “演出设置”改用原生开关样式，Profile、校准流速、演出特效和生命保护修改后 500ms 防抖自动保存；已验收 Profile 首次修改仍需确认并撤销验收。
- 明确区分失败根因：`realtime-20260729-091737` 录像为 56.27 FPS，其失败不是 ALAS 导致；ALAS/MFA 并行是后续 19–31 FPS 异常局的独立稳定性风险。

### 2026-07-26（v0.7.0）

- 增加任务启动生命周期：检查游戏进程、冷启动登录、未知界面最多 60 秒 ESC 恢复，超时后有界重启；识别主页后记录已登录状态。
- 兼容有活动、无活动和活动布局变化的“选择演出”界面，使用模板、OCR、颜色与形状逐级确认自由演出或挑战演出入口。
- 修复自动演出难度覆盖被 Custom Action 丢弃导致 Expert 实际点击 Easy；点击动作改用 MaaFramework 已解析的 `argv.box`。
- 修复自动演出次数仍可用却被误报耗尽：次数状态由无条件 `DirectHit` 改为真实模板匹配，并增加任务轮次 `当前 m/n、已完成 k/n` 日志。
- 主页模板阈值按真实截图校准为 0.82，避免漏识别主页后继续按 ESC 并触发关闭游戏确认。
- 正式实时演奏安全时限由 300 秒提升到 600 秒；超时、生命保护、用户停止、结算识别和异常均记录具体终态原因。
- 区分任务成功、真实失败和用户取消；清除“详情见上一条日志”等含糊信息，真实失败继续传播，人工停止显示“已放弃本次任务”。
- 修复 MFAAvalonia 取消竞态：token 已取消时将 Maa job 失败解释为 `STOPPED`，但保留非取消场景的真实失败。
- 加固定制 MFA 部署：只允许从 `feature/performance-profile-settings` 定制源码构建，验证“演出设置”和 Mirror 启动保护，替换 DLL 前自动备份，禁止回退到同版本官方 DLL。
- 恢复并验收“演出设置”、Profile 参数和 Mirror 启动跳过逻辑；完整 MFA/模拟器实测自动演出、人工停止和启动日志。

### 2026-07-25

- 修复实时演奏黄、粉、蓝键在判定线附近首次出现不触发：亮色或被短暂遮挡的音符首个样本到达判定线时按受保护的首现补救判定一次，松手后 0.4 秒抑制窗继续挡住尾环残影；三首 Hard 录像离线重放确认 0 重复判定、0 松手后误补。
- 同帧邻轨真实双押（双蓝、蓝黄、蓝粉、粉绿、蓝绿）不再被合并为单次判定：并查集分组后，每个具备完整下落轨迹或检测器验证的轨道分别开火，仅无证据碎片仍合并为单点。
- 双绿条配对按下与抬手：同帧开始的两个长条互记配对搭档，任一尾环到达释放线时同帧抬起两个触点；搭档尾部仍高于释放余量时各自独立释放；`reset` 清空配对状态。
- 修复 Normal 演奏仍会失败的根因：判定涟漪与尾环残影在判定线下方约 9 像素处被识别为扁平音符（y≈574、宽 58–66、高 14），在空闲轨道上躲过 0.12 秒重触发窗口与 0.4 秒松手抑制窗后触发幻影补点，失败场次每首约 82–122 次，生命约 85 秒耗尽。现首个样本已位于判定线下方 8 像素及以上的轨道一律按残影吞掉；真机确认 Normal 可稳定演奏。
- 同帧同轨 rescue 不再双双发射：同帧双押按定义位于不同轨道，同轨同时间戳的 rescue 必为碎片，直接吞掉。
- 离线重放验证：失败 Normal 录像瞬发动作 197→107、189→109、298→184，通过场次 146/143/164 不变；Hard 验证集 419 不变、0 重复判定、0 松手后误补；触控规划单测 47/47 通过。
- 合并 `fix/realtime-rescue-chord-pairing`（PR #6）并发布 `v0.6.0`。

### 2026-07-22

- 完成前台应用输入保护真机验收：自动演出在 BangDream 前台时正常完成主页和自由演出入口点击；切换到 `com.android.launcher3` 桌面后，下一次难度点击于门禁处安全失败，随后日志中没有 click、BACK、touch、`stop_app` 或 `start_app`，仅有 MaaFramework 的 `inactive` 收尾。该门禁可阻止向同一模拟器内其他应用发送输入，但 MFA/MaaBanGDream 与 ALAS 仍不得同时运行。
- 修复首次真机前台门禁导致自动演出停在主页：Maa Agent 的跨进程 Controller 代理无法转发 `post_shell`，现优先使用 Controller Shell，并在代理不支持时从同一 Controller 的 `info` 读取 MFA 已绑定的 ADB 路径和序列号，只读执行 `dumpsys window windows`；不硬编码设备、不修改模拟器设置。真实设备输出已确认前台包为 `com.bilibili.star.bili`。
- 新增统一前台包输入门禁：`CommonRecover`、正式演奏预检、实时触控调度和结算 BACK 在每次控制操作前重新确认 BangDream 仍位于前台；应用切换或前台状态未知时安全失败，不向碧蓝航线、桌面或其他应用发送坐标、按键和停止应用操作。新增对应零输入回归测试，并保留全部 `tasker.stopping` 检查。
- 再次定位校准首曲后停在主页的真实根因：第一轮报告其实已成功读取，第二次 `context.run_task()` 复用了普通连续演奏的 `RealtimeLiveRoundGate(max_hit=1)`；MaaFramework 在同一个外层任务中保留该节点命中计数，导致第二轮尚未导航就被跳过。新增无 `max_hit` 的 `RealtimeCalibrationSingleLive` 专用入口，四轮嵌套调用均绕过共享计数器；同时检查嵌套任务真实状态，并永久记录每轮开始、完成、模式、偏移与歌曲 ID。
- 将运行环境从依赖用户级 Python 的仓库 `.venv` 迁移到工作区独立 Miniconda：固定 Miniconda 26.5.3-1、环境名 `maabangdream`、Python 3.12 和 `conda-forge/nodefaults`。`setup.ps1`、`verify.ps1`、`launch-mfa.ps1` 与 MFA 部署配置全部改用该环境。
- 验证脚本将 pytest 临时目录固定到仓库已忽略的 `.local/pytest-<进程号>` 并关闭跨账户缓存，避免 MFA、用户终端和 Codex 使用不同 Windows 账户时再次访问 AppData 或旧 `.pytest_cache` 失败。
- 根据 19:47 校准日志再次修复"首轮结束停在主页"：结算已于 19:49:44 保存，旧逻辑却因文件时间筛选将其排除并在 19:50:57 异常退出。现改为"单轮运行前报告目录快照 → 运行后新增/变化报告"，不再依赖进程时间与文件系统时间一致；校准专用覆盖还会在识别主页后直接结束嵌套单轮，不再回到通用轮次门控等待。
- 使用用户开启调试记录后生成的三份整曲 `trace.jsonl` 定位正式演奏原位补点：长条 `UP` 后约 0.28–0.31 秒，尾环残影被新建为普通音符并触发同轨 `rescue`。新增松手后 0.4 秒残影抑制，但保留具有完整下落轨迹的真实 `crossing`；三份记录离线重放分别由 9/6/9 次降至 0，邻轨重复和长条尾补点继续保持 0。
- 调试记录新增 `events.jsonl` 和异常 PNG：自动保存长条保险松手及松手后再次补救触控的帧；离线重放报告新增 `recorded/replayed_post_release_rescues`。
- 用户已确认正式连续演奏能够正确进入下一轮并结束，挑战演出也已正常完成；这两条流程的主页恢复问题关闭。当前只剩智能校准完整四轮和原位补点修复需要真机复验。
- 根据首次 v0.5.0 集中验收修复三项回归：长绿条不再被固定 6 秒保险提前释放（上限改为 20 秒），普通绿条的预测释放线由 y=570 前移到 y=555；正式/排练结算改为最多 30 次、每 1.5 秒一次 ESC，并在每次输入前重新解析画面。
- 校准首轮结束停在主页的首次根因不是导航，而是嵌套 Maa Pipeline 覆盖没有把临时 `calibration_report` 参数传给播放节点；校准改为直接读取播放 Action 已原子保存的本轮结算 JSON。随后发现文件时间筛选仍会误排除有效报告，已由上面的目录快照方案替换。
- 正式演奏次数为 2 却首轮结束的根因是原实现仅给结算解析 12 秒，正式演出动画尚未出现数字面板便失败；现恢复旧 BDAS 的逐次 ESC 结算策略和 45 秒窗口，成功解析后再交给主页恢复并进入下一轮。
- 挑战演出已完成真机验收，确认共用的正式实时引擎、结算恢复和连续轮次流程正常。
- 修复 MFA 启动流程：明确区分 Git 源码与 MFA 部署副本，新增 `scripts/launch-mfa.ps1` 自动同步 Interface/资源、写入部署路径并重启 MFA。
- 建立本地 `feature/formal-calibration-challenge`，将已通过验收的连续机器人排练快进合入本地 `main`；未推送 GitHub。
- MFA 首页收敛为四项任务，隐藏开发/观察入口但保留底层 Pipeline 和测试。
- 实时演奏增加正式模式：开始前严格匹配 Profile，自动切换正式并关闭自动演出、3D Cut-in 和 3D/MV 显示。
- 新增"三排练一正式"智能校准：每轮读取 FAST/SLOW，单轮最多调整 ±5 ms、总范围 ±250 ms；通过第四首不同歌曲正式验证后原子写入并启用 Profile。
- 新增挑战演出：支持四档点数、五档难度、连续次数，并复用正式实时引擎、Profile 门禁和结算恢复。
- 正式演奏与挑战演出只读取 Profile，不会在演奏中或结算后静默修改延迟。
- 开始本地分支 `feature/realtime-multi-live`：新增"连续机器人排练"，复用稳定的主页恢复、自由演出导航、当前/随机曲目、五档难度和 1–99 次轮次门控。
- 根据用户纠正，连续任务只进入游戏排练模式、强制关闭游戏演示，不再执行 Profile 拒绝门禁；正式演奏暂不暴露给用户。
- 修复黄键作为绿色长条起始头时被拆成 `SKILL TAP + HOLD DOWN` 的错误：检测器现将同轨、同一长条下端的黄头合并为一个长按，同时保留独立黄色短键。
- 新增可选整曲调试录制：每个处理帧写入音符、轨道、几何、生命状态和触控动作 JSONL，并异步保存回放 AVI 与编码丢帧统计；输出只保存在已忽略的 `debug/recordings/`，用于后续训练和离线回归。
- 修复排练点击开始后立即结束：Maa Agent 跨进程 Controller 代理被重复获取，Profile 解析使用第二个代理后引擎仍持有已失效的第一个代理；现整个 Action 只获取并复用一次 Controller。调试开关也改为覆盖完整参数，避免 MFA 的替换式嵌套覆盖意外恢复 Profile 门禁。
- 从 MFA 任务列表隐藏已被连续排练取代的"完整歌曲排练与结算采集"，防止旧勾选状态使两个实时任务排队运行；开发 Pipeline 和契约仍保留。
- 根据连续排练的整曲调试轨迹修复重复触控：黄键多点并非缺少数据，而是短暂丢失后新 Track 再次触发补救；现对同一处理帧启用同轨/邻轨共享判定，跨帧 120ms 仅抑制追踪重建产生的 `rescue`，不会吞掉具有正常运动过线轨迹的密集音符；Flick 优先覆盖相邻 Tap，并接通此前未使用的 `_last_trigger` 去重状态。
- 删除长条 `UP` 时主动补点附近音符的 `linked-tail` 行为；长条尾部现在只松开，不再次点击，并在松开后设置 250ms 同轨重启冷却，避免残留绿条产生 `DOWN → UP` 抖动。对用户验收录制离线回放后，总动作由 411 降至 312，短窗同/邻轨重复判定由 45 降至 0，长条尾补点由 33 降至 0。
- 新增 `scripts/replay_realtime_trace.py`，可直接用调试模式生成的 `trace.jsonl` 离线重放规划器，后续识别训练和触控回归无需反复消耗真机演出。
- 实时 Agent 每轮完成演奏、逐次 ESC 识别结算并记录 FAST/SLOW，Pipeline 再安全返回主页进入下一轮；用户停止、生命归零、解析失败或环境漂移均不继续下一轮。
- 根据用户已明确通过的 Hard 粉色 Flick 真机验收，生成本机 Hard Profile；这些 Profile 仍保留给独立校准/正式模式研究，不再阻塞机器人排练。
- 开始本地分支 `feature/realtime-result-calibration`：新增完整歌曲排练入口，使用已验收 Easy Profile 最长运行 300 秒。
- 新增演出结束状态机：必须先确认有效生命条存活，再连续 120 帧（约 2 秒）不可见才判定离开演出；开场、普通页面和短暂遮挡不会误结束。
- 完整歌曲结束、停止、生命归零或异常时仍统一释放触点；正常结束后将结算画面保存到已忽略的 `screencap/`，供后续 FAST/SLOW 解析建立真实回放测试。
- 用户完成首次完整 Easy 歌曲：7561 帧、389 次动作、正常识别演出结束。根据实际页面流程，结束后改为逐次发送 ESC、每次重新截图解析，识别到结算即停止继续返回。
- 已迁移固定 1280×720 结算数字解析器并用模拟器当前画面验证：PERFECT 170、GREAT 42、GOOD 0、BAD 0、MISS 3、FAST 33、SLOW 9，置信度 0.80；当前只建议将时序偏移从 0 调至 -5 ms，不自动修改已验收 Profile。
- 开始本地分支 `feature/realtime-profile-calibration`：将已通过的 Easy 机器人排练转化为本机 Profile 授权，不推送 Profile 文件。
- 修复 Profile 草稿仍调用 MaaFw 5.10.2 Shell/Controller 属性的问题；现与排练一致，从实际截图读取分辨率，DPI 使用锁定的本机参数 240。
- 新增 Profile 原子验收、验收时间审计、难度/环境二次核对和最新已验收 Profile 解析；未验收、难度错误或任一环境字段漂移均拒绝运行。
- 新增"Profile 驱动排练（Easy，30 秒）"入口，触控参数只从最新匹配的已验收 Profile 读取；当前等待一次真机门禁后再进入完整歌曲与结算反馈。
- 修复 Profile 驱动排练约一秒退出：生命保护此前可在尚未确认见过存活生命条时，因普通页面连续三帧零读数而误报死亡；现必须先连续确认存活，且确认前只截图、不发送任何触控。
- Easy Profile 驱动排练已通过用户真机验收；Hard 试跑发现粉色 Flick 被游戏判成普通点击，根因是原生 Down/Move/Up 在同一输入采样窗口内瞬时完成。Flick 现改为 36 ms 三段同步上滑，双 Flick 仍保持同相位，且不释放并行长按触点；修复后已通过用户 Hard 真机验收。
- 开始本地分支 `feature/realtime-note-tracking`：迁移同轨多音符 Track ID、唯一帧速度回归和短时轨迹记忆；观察日志增加 Tap/Skill/Flick 唯一轨迹计数，仍不触控。
- 连续迁移触控规划与 MaaTouch 协议核心：覆盖和弦去重、Flick、双长条、尾端释放、预测释放、六秒强制释放、连接音符同批提交以及停止时全部触点复位；当前仍未连接真实 MaaTouch 服务。
- MaaFramework 5.10.2 已原生提供多点 `TouchDown/TouchMove/TouchUp`，新增 Controller 调度适配层，停止、异常和关闭时都会尝试释放所有活动触点；优先采用框架原生输入，不依赖本机 ADB 路径。
- 新增尚未暴露到 MFA 任务列表的实时引擎核心，串联取帧、检测、规划和原生触控；正常结束、用户停止和内部异常均通过 `finally` 执行规划器复位与触点关闭。
- 新增 Profile 草稿任务：通过 MaaController 自动读取真实分辨率和 Android DPI，结合游戏帧率、画质、音符速度与难度生成本机 JSON；草稿强制为 `accepted=false`，不能直接授权正式演奏。
- 迁移生命条保护为同步 BGR 检测与三帧确认状态机：生命条不可见只记为未知，连续三帧接近零才判定死亡，避免加载页或结算页误判。
- 新增首个单一验收入口"机器人排练（Easy，30 秒）"：严格限制 1280×720、DPI 240、60 FPS、standard 画质和音符速度 2.0，环境漂移直接拒绝；正常、停止、生命归零或异常退出均释放全部触点。
- 修复机器人排练一秒结束：MaaFw Python Binding 5.10.2 漏声明 `MaaControllerPostShell` 的 ctypes 参数，64 位 Controller 句柄被截断；项目兼容层现显式绑定完整句柄、命令和超时类型。
- 自定义排练/Profile 回调现捕获并记录异常后返回失败，避免 ctypes 吞掉 Python 异常并让 MFA 将失败任务误报为成功。
- 排练路径不再调用 Agent 转发 Controller 的 Shell API；DPI 使用已锁定的 MFA 本机配置参数 240，分辨率由首张实际截图确认，避免 Shell/Controller 属性兼容缺陷阻塞实时引擎。
- 首次 30 秒机器人排练完成 1801 帧和 49 次动作；针对一枚短暂可见的黄色 Skill 音符漏击，启用"已识别音符首次近线补救"，仍保留颜色、几何、轨道校验和追踪去重；修复后已通过用户真机验收。
- 旧项目没有持续使用 Git；后续将其整体作为参考源码审查，不再以旧工作树的 tracked/untracked 状态判断可迁移性，但继续排除日志、截图、本机配置和缓存。
- 开始本地分支 `feature/realtime-note-observe`：只迁移音符视觉检测，不迁移未跟踪的旧多音符跟踪器，也不发送触控。
- 从旧仓库干净 `HEAD` 导入七轨 Tap、Skill、Hold、Flick 检测与离线几何测试，并记录 GPL-3.0-only 来源映射；运行依赖使用无 GUI 的 OpenCV。
- 新增 MaaController 实时音符观察入口，按 60 FPS 上限处理 BGR 截图并输出类型/轨道统计；任务不调用任何控制器输入 API。
- 用户分别在 Expert、Hard、Easy 完成三次观察，类型数量随难度递减且七轨总体均有覆盖；首次实测处理率约 32 FPS，定位并修复节流时间轴累计截图耗时后，复验达到 601 帧/10 秒（约 60.1 FPS），无停止或任务异常。
- 开始 `feature/realtime-profile-foundation`：重新实现实时演奏 Profile v1，不复制旧仓库中存在未提交修改的 Profile 代码与测试。
- 新增强类型环境签名，固定检查分辨率、DPI、游戏帧率、演出画质与音符流速；五项中任意一项变化都会拒绝使用旧 Profile。
- 新增 Profile 路径隔离、原子写入、版本检查、参数边界及"必须经用户真机验收"门禁；提供不可直接用于正式演奏的示例文件。
- 修复本机缺失的 Python 3.12.10 安装并重建项目 `.venv`；排查到一次停止任务后的 Agent 残留，后续实时任务必须增加进程与触点清理回归。
- 新增 `RealtimeObserve` Custom Action 与 MFAAvalonia 任务入口，使用 MaaController 做 5 秒只观察取帧并统计延迟/超时；单元与 Pipeline 契约保证该任务没有点击、滑动或应用控制节点。
- 用户完成两次零触控取帧验收：分别为 158.80 FPS / 156.80 FPS，最大取帧耗时均为 16 ms，超时帧和无效帧均为 0。
- 清理 MaaFramework 5.10.2 中已弃用的 `Toolkit.init_option` 调用，改用 `Tasker.set_log_dir`；MFA 打开期间 Agent 保持活动连接，关闭 MFA 后才应退出。
- 按用户决策取消指定歌曲功能，仅保留当前曲目和每轮随机选曲两种模式。
- 开始 `feature/multi-live-difficulty`：新增 1–99 轮次数输入、五档难度选择和原生 `max_hit` 轮次门控。
- 多轮结算后先安全回主页再进入下一轮；达到目标次数后正常完成，自动演出次数耗尽仍立即停止。
- 修复多轮第二次开始可能随机点击到按钮外的问题；开始节点改为只点击已识别按钮框内部。
- 新增启动回主页门禁：任务可从大多数普通页面启动，未识别主页时每 1.5 秒发送 ESC，并在登录、关闭或剧情安全节点出现时优先点击；超时后最多重启两次。
- 用户已完成 v0.3.0 真机验收，确认非主页启动恢复、Expert 难度和连续两轮流程通过。
- 在 `feature/auto-live-foundation` 分支新增当前歌曲单轮自动演出 Pipeline。
- 从旧仓库 `HEAD` 导入准备、自动演出状态、开始、结算、奖励和剧情模板，并记录 SHA-256 来源清单；未复制旧仓库工作区修改。
- 开始按钮只能从"自动演出已开启"节点到达；耗尽状态优先并直接停止；关闭状态最多尝试三次。
- 演出结果等待上限设为 300 秒，正常演出等待不会触发 60 秒未知页恢复。
- 扩展 `CommonRecover`，支持安全节点点击、逐次新截图和每项控制操作前的停止检测。
- 新增自动演出 Pipeline 契约、模板来源哈希和停止安全性测试；当前共 15 项自动测试。
- 修正选曲确认点击区域，并在真实雷电模拟器完成两轮验收：自动演出关闭时先开启再开始，已开启时直接开始；两轮均识别结算并返回主页。
- 验收日志确认演出等待期间没有 BACK，BACK 仅在结算恢复阶段发出；停止失败验收后没有继续控制游戏。
- 次数耗尽状态未在真机构造，本版本仅通过来源模板回放约束和 Pipeline 契约验证。
- 新增 v0.2.0 发布说明。
- 新增精确运行时组合检查并接入 `scripts/verify.ps1`。

### 2026-07-21

- 从旧项目迁出独立 MaaFramework 工程并建立 `main` 分支。
- 安装并验证 Python 3.12、MaaFw 5.10.2、MFAAvalonia 2.12.0 和 .NET 10。
- 完成 Project Interface V2、Python Agent、Pipeline、最小页面闭环和基础 `CommonRecover`。
- 创建公开 GitHub 仓库并发布 `v0.1.0` 开发预览。
- 项目由 `BDAS-Maa` 统一更名为 `MaaBanGDream`，通过 PR #1 合并。
- 将 README 确立为项目进度、修改和后续计划的统一记录入口。
